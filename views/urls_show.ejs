<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
    <title>URL - TinyApp Example</title>
  </head>

  <body>
    <%- include('partials/_header', { username: username }) %>

    <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <h5 class="card-title">TinyURL for: <%= longURL %></h5>
        <p class="card-text">
          Short URL ID: <a href="/u/<%= id %>"><%= id %></a>
        </p>
        <!-- form to update longURL-->
        <form method="POST" action="/urls/<%= id %>/update">
          <div class="form-group">
            <label for="updatedLongURL">Update Long URL:</label>
            <input type="text" class="form-control" id="updatedLongURL" name="updatedLongURL" value="<%= longURL %>">
          </div>
          <button type="submit" class="btn btn-primary">Update</button>
        </form>
      </div>
      <div class="card-footer text-muted"></div>
    </div>
    <!-- Bootstrap JS -->
    <!-- ... (unchanged) -->
  </body>
</html>